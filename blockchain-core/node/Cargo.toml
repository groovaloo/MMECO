[package]
name = "node"
version = "0.1.0"
edition = "2021"

[dependencies]
# CLI
clap = { version = "4.4.10", features = ["derive"] }

# Runtime
runtime = { path = "../runtime" }

# Service & Client

# Consensus

# Network

# RPC

# CLI & Utilities

# Storage

# Key Management
sp-keyring = "34.0.0"
sp-core = "34.0.0"

# Runtime Dependencies
