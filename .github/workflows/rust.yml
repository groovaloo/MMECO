[package]
name = "mmeco-runtime"
version = "0.1.0"
edition = "2021"
publish = false

[dependencies]
# SDK com features necessárias + pallet-timestamp
polkadot-sdk = { workspace = true, features = [
    "runtime",
    "pallet-aura",
    "pallet-grandpa",
    "pallet-balances",
    "pallet-timestamp"
] }

# Codificação base
parity-scale-codec = { workspace = true }
scale-info = { workspace = true }

# Paletes personalizadas
pallet-reputation = { path = "../pallets/reputation", default-features = false }
pallet-projects = { path = "../pallets/projects", default-features = false }
pallet-governance = { path = "../pallets/governance", default-features = false }

[build-dependencies]
substrate-wasm-builder = { workspace = true }

[features]
default = ["std"]
std = [
    "parity-scale-codec/std",
    "scale-info/std",
    "polkadot-sdk/std",
    "pallet-reputation/std",
    "pallet-projects/std",
    "pallet-governance/std",
]
runtime-benchmarks = [
    "polkadot-sdk/runtime-benchmarks",
    "pallet-reputation/runtime-benchmarks",
]
try-runtime = [
    "polkadot-sdk/try-runtime",
    "pallet-reputation/try-runtime",
]

